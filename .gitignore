# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# direnv (per-developer, never committed)
.envrc

# OS / editor
.DS_Store
.vscode/
.idea/

# ML artifacts (regenerated; don't commit by default)
mlruns/
*.joblib
pdm-ai-engine/*.json
pdm-ai-engine/Data/

# Exception: v1 baseline models live in pdm-ai-engine/models/ for clone-and-run.
# Day 7 onward, new models go to MLflow's artifact store (not git).
!pdm-ai-engine/models/*.joblib
!pdm-ai-engine/models/*.json

# Simulator artifacts
machine-simulator/backend/simulation_v2.db
*.db
*.sqlite
*.sqlite3

# Working docs — internal to the sprint, not for GitHub
CLAUDE.md
SPRINT.md
audit_notes.md
instruction.md
INTERVIEW_PREP.md
RESUME_UPDATES.md

# Frontend
node_modules/
dist/
build/
